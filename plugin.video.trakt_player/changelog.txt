================================================================================
  Trakt Player Changelog
  by zeus768
================================================================================

v2.1.3 (April 2026)
================================================================================
  * Fixed debrid and Trakt auth not persisting - all settings now use fresh
    Addon() instances on every read/write so setSetting changes are never lost
    to stale in-memory cache (fixes RD, PM, AD, TB, and Trakt auth all dropping)
  * Fixed Real-Debrid OAuth URL (was /rest/1.0/oauth, now /oauth)
  * Fixed Premiumize OAuth URL (was /api/token, now /token)

v2.1.2 (April 2026)
================================================================================
  * Fixed Real-Debrid pair code not showing (OAuth URL was wrong - used /rest/1.0/oauth instead of /oauth)
  * Fixed Premiumize pair code not showing (OAuth URL was wrong - used /api/token instead of /token)
  * Added OAUTH_URL constant for RD and PM to separate API vs auth endpoints

v2.1.1 (April 2026)
================================================================================
  * Compact Buy Me a Beer - single QR + Ko-fi dialog
  * Fixed Trakt API Cloudflare 403 - custom User-Agent header
  * Fixed zip versioning mismatch
  * All menus/submenus use local addon fanart and icon only

v2.1.0 (February 2026)
================================================================================
  * Fixed Kodi Service IndexError (sys.argv[1] crash in background)
  * Bumped version, cleaned all Umbrella references

v2.0.0 (February 2026)
================================================================================

  COMPLETE REWRITE - 100% native urllib, zero requests dependency

  CORE
  ----
  * Click-and-Play (auto <=1080p, no source dialog)
  * Trakt Scrobbling (background service)
  * Up Next (auto-play next episode)
  * 5 torrent scrapers (1337x, YTS, EZTV, TorrentGalaxy, TPB)

  BROWSE & DISCOVERY
  ------------------
  * Continue Watching, Recommendations, Calendar, History
  * Popular Lists, Anticipated, Related Content
  * AI Discovery (12 mood presets + custom vibe)
  * Discovery Feed (Trailer Scroll) - 6 modes

  SOCIAL & LISTS
  --------------
  * Rate 1-10, Add to Watchlist, Add to Custom List
  * Custom Lists (create/browse/delete)
  * Friends Activity Feed (live + history)

  ACCOUNT & STATUS
  ----------------
  * User Stats Dashboard
  * Debrid Account Status
  * Buy Me a Beer - Ko-fi donation

  DEBRID SUPPORT
  --------------
  * Real-Debrid, Premiumize, AllDebrid, TorBox
  * Cached Torrent Indicator
